from app.screen import Screen, WIDTH, HEIGHT, GRID_UNIT, blk
from PIL import Image, ImageDraw, ImageFont

class Widget():
    def __init__(self, position=(0, 0), size=(blk(10), blk(10)), container=True):
        
        pass

